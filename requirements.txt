# === Core API ===
fastapi==0.116.1
uvicorn==0.30.6           # no [standard] -> avoids Rust watchfiles
python-dotenv>=1.0.1

# === RAG stack ===
chromadb==1.0.20
sentence-transformers==3.0.1
transformers==4.43.4

# === JSON accelerator (binary wheel available) ===
orjson==3.10.7            # widely available manylinux wheel

# === PyTorch (CPU) — let pip pick compatible wheel on Render
--extra-index-url https://download.pytorch.org/whl/cpu
torch
